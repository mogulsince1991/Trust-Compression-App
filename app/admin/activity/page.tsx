import { PlatformActivityDashboard } from "@/components/platform-activity-dashboard";

export const metadata = {
  title: "Platform Activity | Trust Compression",
};

export default function PlatformActivityPage() {
  return <PlatformActivityDashboard />;
}
